-- -----------------------------------------------------
-- Table `scheduled_delete_info` exam_uuid column size
-- -----------------------------------------------------

ALTER TABLE `scheduled_delete_info`
MODIFY `exam_uuid` VARCHAR(255) NULL
;